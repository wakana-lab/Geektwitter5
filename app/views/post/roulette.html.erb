<!DOCTYPE html>
<html lang="ja">
<head>

    
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ルーレット結果</title>

 
</head>

<body class="flex items-center justify-center min-h-screen">
  <div class="container mx-auto p-4 flex flex-col items-center justify-center space-y-8">
    <h1 class="text-4xl font-extrabold text-blue-700 mb-6 text-center">ルーレット結果</h1>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6 w-full max-w-4xl">
      <% @results.each do |result| %>
        <div class="card p-6 flex flex-col items-center space-y-4">
          <h2 class="text-2xl font-semibold text-gray-800">質問 <%= result[:question_number] %></h2>
          <p class="text-5xl font-bold text-green-600 animate-pulse-once"><%= result[:choice] %></p>
          <p class="text-gray-600 text-lg">が選ばれました！</p>
        </div>
      <% end %>
    </div>
    <%= link_to '一覧表示', tweets_path %>
    <div class="mt-8">
      <%= link_to 'もう一度ルーレットを回す', roulette_post_path, class: 'roulette-button' %>
    </div>
  </div>
</body>
</html>
